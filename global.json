{
  "sdk": {
    // This repo targets .NET 10.0.
    // Install the .NET 10 SDK from https://aka.ms/dotnet/download to build.
    // Allow using the latest 10.0 patch (e.g. 10.0.201) when available.
    "version": "10.0.201",
    "rollForward": "latestPatch"
  }
}
